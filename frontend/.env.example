# Leave empty to use Next.js rewrite proxy to local FastAPI.
NEXT_PUBLIC_API_URL=
API_PROXY_ORIGIN=http://127.0.0.1:8000

# Optional Google Analytics (never commit a real .env.local)
NEXT_PUBLIC_GA_MEASUREMENT_ID=
